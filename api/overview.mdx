---
title: "Visão geral da API"
description: "Base URL, duas camadas (legado /api e contrato /api/v1) e limites reais dos planos B2B."
sidebarTitle: "Overview"
---

## Base URL

```
https://api.nzochain.com/api
```

Todas as rotas abaixo são relativas a este prefixo. **Não** uses `https://api.nzochain.com/v1` sem `/api`.

## Duas camadas

| Camada | Prefix | Uso |
|---|---|---|
| **API v1 Firewall** (recomendada para produtos novos) | `/v1/...` | Contrato estável: `decision`, `riskLevel`, `reasons` |
| Legado | `/risk/scan`, simulação, etc. | Continua a existir; a app B2C e integrações antigas usam-no |

Começa por [API v1](/api/v1). Autenticação: [X-API-Key](/api/authentication).

## Planos e volume

| Plano | Calls / mês | Firewall | Simulate / mês | Threats | Overage / call |
|---|---|---|---|---|---|
| Free | 1 000 | Não | 0 | Não | $0.01 |
| Starter | 25 000 | Sim | 100 | Não | $0.004 |
| Growth | 100 000 | Sim | 2 000 | Standard | $0.002 |
| Business | 500 000 | Sim | 10 000 | Completo | $0.001 |
| Enterprise | Ilimitado | Sim | Ilimitado | Completo | Contrato |

Calls extra **não** devolvem bloqueio de quota de volume: o overage é registado. Ver [erros](/api/errors).

## Envelope JSON

Respostas de sucesso (interceptor):

```json
{ "success": true, "data": { } }
```

`data` na v1 inclui tipicamente `riskScore`, `riskLevel`, `decision`, `reasons`.
